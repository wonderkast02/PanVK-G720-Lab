# Logs
Small text/JSON evidence may be committed. Large artifacts stay outside Git; record SHA256/size/provenance. Bootstrap imports matching local reports into `history/raw-import/` with `INDEX.tsv`.
